services:
  web:
    build: .
    ports:
      - "127.0.0.1:${PORT:-8503}:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL:-/data/badminton.db}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - ./data:/data
    restart: unless-stopped
