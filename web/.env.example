# 管理后台口令，必填
ADMIN_PASSWORD=your-secure-password

# 可选:容器内 SQLite 路径(默认 /data/badminton.db,对应宿主机 web/data/badminton.db)
# DATABASE_URL=/data/badminton.db

# 可选:宿主机端口(默认 8503,对齐现有 Apache 反代配置)
# PORT=8503
